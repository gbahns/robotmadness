{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf prisma/migrations && npx prisma generate --schema=./prisma/schema.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --schema=./prisma/schema.prisma --skip-generate && npm start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}