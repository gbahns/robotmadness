{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema=./prisma/schema.postgresql.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --schema=./prisma/schema.postgresql.prisma --skip-generate && npm start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}